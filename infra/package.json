{
  "name": "@repo/infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "preview": "just run dev pulumi preview",
    "up:dev": "just deploy dev",
    "up:prd": "just deploy prd",
    "destroy": "just run dev pulumi destroy --yes",
    "test": "vitest run --passWithNoTests",
    "check-types": "tsgo --noEmit"
  },
  "dependencies": {
    "@ozby/wrangler-sync": "^0.1.0",
    "@pulumi/cloudflare": "^6.14.0",
    "@pulumi/pulumi": "^3.232.0",
    "@pulumiverse/doppler": "^0.9.0",
    "@webpresso/db-branching-neon": "catalog:",
    "@webpresso/runtime": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:tooling",
    "@webpresso/agent-tsconfig": "catalog:",
    "@webpresso/agent-vitest": "catalog:",
    "tsx": "^4.21.0",
    "vitest": "catalog:tooling"
  }
}
