# Dependency directories
node_modules/

# Build output
dist/
build/
.turbo/
.clinic/
# Logs
logs
*.log
npm-debug.log*

# Environment variables
.env
.env.local
.env.*.local

# Client per-env build variables (not secrets, but not reproducible cross-machine)
apps/client/.env.dev
apps/client/.env.prd

# Coverage directory
coverage/

# Editor directories and files
.idea/
.vscode/
*.swp
*.swo

# OS-specific
.DS_Store
Thumbs.db

# Stale editor / generated backup suffixes
*.new

# MCP / tool session artifacts
.playwright-mcp/
!.omx/
.omx/*
!.omx/contracts/
!.omx/contracts/**
!.omx/plans/
!.omx/plans/**
!.omx/state/
.omx/state/*
!.omx/state/lifecycle/
!.omx/state/lifecycle/**
.codex/.tmp/
.codex/cache/
.codex/log/
.codex/memories/
.codex/sessions/
.codex/shell_snapshots/
.codex/tmp/
.codex/.personality_migration
.codex/auth.json
.codex/history.jsonl
.codex/hooks.json
.codex/installation_id
.codex/logs_2.sqlite*
.codex/models_cache.json
.codex/state_5.sqlite*
.codex/version.json
.claude/logs/
.claude/projects/
.claude/settings.json
.claude/tmp/
.claude/*.local.*

# Stryker incremental cache
.stryker-incremental.json

# Git worktrees (parallel blueprint lanes)
.worktrees/

# Wrangler local dev state and secrets
.wrangler/
.dev.vars
.codex/*
!.codex/agents/
!.codex/agents/**
!.codex/skills/
!.codex/skills/**
.codex/skills/.system/**
!.codex/prompts/
!.codex/prompts/**

# Vitest per-run reports (machine-local)
.test-reports/
.neon
scripts/probes/consistency-lab/verdicts.jsonl

# Claude Code agent runtime worktrees (locked by the agent host)
.claude/worktrees/

# Stryker mutation reports (per-run artifact, ~1 MB)
apps/workers/reports/

# Local extraction of github.com:webpresso/tooling — installed via pnpm catalog
# git pointer; never commit a vendored copy.
/tooling/
.gstack/
.webpresso/
.npmrc

# >>> managed by @webpresso/agent-kit (rule-sync)
.agent/rules/
.cursor/rules/
.windsurf/rules/
.claude/rules/
.gemini/commands/
.codex/agents/
# <<< managed by @webpresso/agent-kit (rule-sync)

# >>> managed by @webpresso/agent-kit (skill-sync)
.agent/skills/
.cursor/skills/
.windsurf/skills/
.claude/skills/
.gemini/commands/
.codex/agents/
# <<< managed by @webpresso/agent-kit (skill-sync)

# agent-kit compile cache (always regeneratable)
.agent/.compile-manifest.json
.agent/.compile.hash
.agent/.blueprints.db
.agent/.blueprints.lock
.agent/.rotation-log.jsonl
.agent/.tail-hint-history.jsonl
.rulesync/
